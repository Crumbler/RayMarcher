<Page
    x:Class="RayMarchEditor.SaveImagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RayMarchEditor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <Style TargetType="NumberBox">
            <Setter Property="NumberFormatter" Value="{StaticResource decimalFormatter}"/>
            <Setter Property="Minimum" Value="1"/>
            <Setter Property="Maximum" Value="10000"/>
        </Style>
    </Page.Resources>

    <StackPanel HorizontalAlignment="Left">
        <NumberBox Header="Image width" HorizontalAlignment="Left"
                   Value="{x:Bind ImageWidth, Mode=TwoWay}"/>
        <NumberBox Header="Image height" HorizontalAlignment="Left"
                   Value="{x:Bind ImageHeight, Mode=TwoWay}"/>
    </StackPanel>
</Page>
