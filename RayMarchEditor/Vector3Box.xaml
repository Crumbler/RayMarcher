<UserControl
    x:Class="RayMarchEditor.Vector3Box"
    x:Name="root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RayMarchEditor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="NumberBox">
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="8"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Vertical">
        <NumberBox Value="{Binding X, ElementName=root, Mode=TwoWay}"/>
        <NumberBox Value="{Binding Y, ElementName=root, Mode=TwoWay}"/>
        <NumberBox Value="{Binding Z, ElementName=root, Mode=TwoWay}"/>
    </StackPanel>
</UserControl>
